<Page x:Name="pageRoot"
    x:Class="scare.pumpkin.ui.Views.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:scare.pumpkin.ui.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:uc="using:scare.pumpkin.ui.Views"
    xmlns:prism="using:Prism.Windows.Mvvm"
    mc:Ignorable="d"
    prism:ViewModelLocator.AutoWireViewModel="true">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Row="0" Grid.Column="0" Orientation="Horizontal">
            <Button Command="{Binding NavigateCommand}">Pumpkin</Button>
            <Button Command="{Binding NavigateControlCommand}">Control</Button>
        </StackPanel>
    </Grid>
</Page>
